pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
        stage('build') {
            steps {
		cleanWs()
            }	
            steps {
	    	sh "ls -lta"
                sh "date >> data.txt"
		sh "ls -lta"
           }
        }
}
}
